Drop these files and folders into an emptied 'presets' folder on the MicroSD.  
The samples used by these presets can be found in the '[master] samples' folder.
If you don't copy the samples, te preset will use whatever it sees first. 