Impulse responses that are used by the Convolver algorithm.

These go in 'impulses' on the root of the MicroSD card. 
Copy the contents of one of these packs into root/impulses.

By default, both included packs populate root/impulses.